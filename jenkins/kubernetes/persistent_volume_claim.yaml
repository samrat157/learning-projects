---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-pv-claim
  namespace: jenkins
spec:
  storageClassName: jenkins-local-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 30Gi